# Live Betting Demo

This is a simple HTML/JS demo for a live progressive betting game using Supabase.

## Steps

1. Create a Supabase project.
2. Create `bets` and `daily_results` tables.
3. Replace Supabase URL and anon key in `supabase_config.js`.
4. Deploy to Vercel or open `index.html` in browser.
